---
interface Props {
  quote: string;
  name: string;
  role: string;
  company: string;
  index?: number;
}

const { quote, name, role, company, index = 0 } = Astro.props;
const initials = name.split(' ').map(n => n[0]).join('');
---

<blockquote class="testimonial-item" style={`--delay: ${index * 0.15}s`}>
  <span class="frame-right" aria-hidden="true"></span>
  <span class="frame-bottom" aria-hidden="true"></span>
  <span class="frame-left" aria-hidden="true"></span>
  <div class="testimonial-quote-mark" aria-hidden="true">"</div>
  
  <div class="testimonial-content">
    <p class="testimonial-quote">{quote}</p>
    
    <footer class="testimonial-author">
      <div class="author-avatar" aria-hidden="true">
        <span class="avatar-initials">{initials}</span>
      </div>
      <div class="author-info">
        <cite class="author-name">{name}</cite>
        <p class="author-role">{role} @ {company}</p>
      </div>
    </footer>
  </div>
  
  <div class="testimonial-border" aria-hidden="true"></div>
</blockquote>

<style>
  .testimonial-item {
    position: relative;
    background: var(--bg);
    border: 3px solid var(--border);
    padding: var(--space-xl);
    opacity: 0;
    transform: translateY(30px) rotate(-1deg);
    animation: testimonial-enter 0.6s ease forwards;
    animation-delay: var(--delay);
    margin: 0;
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    overflow: hidden;
    isolation: isolate;
  }
  
  .testimonial-item:nth-child(even) {
    transform: translateY(30px) rotate(1deg);
  }
  
  @keyframes testimonial-enter {
    to {
      opacity: 1;
      transform: translateY(0) rotate(0deg);
    }
  }
  
  .testimonial-item:hover {
    transform: translate(-6px, -6px);
    box-shadow: 6px 6px 0 var(--border);
  }
  
  /* Keep the rotation for even items on hover */
  .testimonial-item:nth-child(even):hover {
    transform: translate(-6px, -6px) rotate(0deg);
  }
  
  /* Hover frame - top line */
  .testimonial-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 0;
    height: 3px;
    background: var(--text);
    transition: width 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    z-index: 4;
  }

  .testimonial-item:hover::before {
    width: 100%;
  }

  /* Right line */
  .testimonial-item .frame-right {
    position: absolute;
    top: 0;
    right: 0;
    width: 3px;
    height: 0;
    background: var(--text);
    transition: height 0.3s cubic-bezier(0.16, 1, 0.3, 1) 0.08s;
    z-index: 4;
  }

  .testimonial-item:hover .frame-right {
    height: 100%;
  }

  /* Bottom line */
  .testimonial-item .frame-bottom {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 0;
    height: 3px;
    background: var(--text);
    transition: width 0.3s cubic-bezier(0.16, 1, 0.3, 1) 0.16s;
    z-index: 4;
  }

  .testimonial-item:hover .frame-bottom {
    width: 100%;
  }

  /* Left line */
  .testimonial-item .frame-left {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 3px;
    height: 0;
    background: var(--text);
    transition: height 0.3s cubic-bezier(0.16, 1, 0.3, 1) 0.24s;
    z-index: 4;
  }

  .testimonial-item:hover .frame-left {
    height: 100%;
  }
  
  .testimonial-item:hover .testimonial-border {
    opacity: 1;
  }

  .testimonial-quote-mark {
    position: absolute;
    top: -0.2em;
    left: var(--space-lg);
    font-size: 6rem;
    font-weight: 700;
    color: var(--text);
    opacity: 0.1;
    line-height: 1;
    pointer-events: none;
    font-family: Georgia, serif;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial-item:hover .testimonial-quote-mark {
    opacity: 0.2;
    transform: scale(1.05) translateY(-4px);
  }

  .testimonial-content {
    position: relative;
    z-index: 1;
  }

  .testimonial-quote {
    font-size: clamp(0.9375rem, 2vw, 1rem);
    line-height: 1.7;
    color: var(--text);
    font-style: normal;
    margin-bottom: var(--space-xl);
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial-item:hover .testimonial-quote {
    transform: translateX(6px);
  }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding-top: var(--space-lg);
    border-top: 2px solid var(--border);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial-item:hover .testimonial-author {
    transform: translateX(6px);
    border-color: var(--text);
  }

  .author-avatar {
    width: 48px;
    height: 48px;
    background: var(--text);
    border: 2px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial-item:hover .author-avatar {
    transform: scale(1.05);
    border-color: var(--text);
  }

  .avatar-initials {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--bg);
    letter-spacing: 0.05em;
    font-family: var(--font-mono);
  }

  .author-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .author-name {
    font-size: 0.9375rem;
    font-weight: 700;
    color: var(--text);
    font-style: normal;
    transition: color 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .author-role {
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    font-family: var(--font-mono);
    margin: 0;
    transition: color 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial-item:hover .author-role {
    color: var(--text);
  }
  
  .testimonial-border {
    position: absolute;
    inset: 0;
    border: 3px solid var(--text);
    opacity: 0;
    transition: opacity var(--transition-fast);
    pointer-events: none;
  }
  
  /* Responsive */
  @media (max-width: 640px) {
    .testimonial-item {
      padding: var(--space-lg);
    }
    
    .testimonial-item:hover {
      transform: translate(-4px, -4px);
    }
    
    .testimonial-quote-mark {
      font-size: 4rem;
    }
  }
  
  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .testimonial-item {
      animation: none;
      opacity: 1;
      transform: none;
    }
    
    .testimonial-item:nth-child(even) {
      transform: none;
    }
  }
</style>
