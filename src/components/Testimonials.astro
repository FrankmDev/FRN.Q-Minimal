---
const testimonials = [
  {
    quote: "Francisco transformó nuestra visión en realidad. Su atención al detalle es incomparable.",
    author: "María G.",
    role: "CEO, Nexus"
  },
  {
    quote: "El mejor desarrollador con el que he trabajado. Código limpio, diseño impecable.",
    author: "Carlos R.",
    role: "Product Lead"
  },
  {
    quote: "Nuestra web carga en 0.8s y las conversiones subieron 40%. Increíble trabajo.",
    author: "Ana M.",
    role: "Marketing Dir."
  }
];
---

<section class="testimonials" id="testimonios" aria-labelledby="testimonials-title">
  <div class="container">
    <header class="section-header reveal">
      <span class="section-label">Testimonials</span>
      <h2 id="testimonials-title" class="section-title">Client Words</h2>
    </header>
    
    <div class="testimonials-grid" role="list">
      {testimonials.map((t, i) => (
        <blockquote class="testimonial reveal" style={`transition-delay: ${i * 0.15}s`} role="listitem">
          <span class="testimonial-border-bottom" aria-hidden="true"></span>
          <span class="testimonial-border-left" aria-hidden="true"></span>
          <div class="quote-mark" aria-hidden="true">"</div>
          <p class="quote-text">{t.quote}</p>
          <footer class="quote-footer">
            <cite class="quote-author">{t.author}</cite>
            <span class="quote-role">{t.role}</span>
          </footer>
        </blockquote>
      ))}
    </div>
  </div>
</section>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });
  
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>

<style>
  .testimonials {
    background: var(--bg);
    padding: var(--space-4xl) 0;
    position: relative;
  }
  
  .testimonials::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: repeating-linear-gradient(
      90deg,
      var(--border) 0,
      var(--border) 20px,
      transparent 20px,
      transparent 40px
    );
  }

  .section-header {
    margin-bottom: var(--space-2xl);
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    flex-wrap: wrap;
    gap: var(--space-sm);
  }

  .section-label {
    font-size: 0.875rem;
    color: var(--text-muted);
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .section-title {
    font-size: clamp(2rem, 6vw, 3.5rem);
    font-weight: 600;
    letter-spacing: -0.03em;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
  }

  @media (min-width: 768px) {
    .testimonials-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .testimonial {
    margin: 0;
    padding: var(--space-xl);
    background: var(--bg-alt);
    border: 2px solid var(--border);
    position: relative;
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    overflow: hidden;
    isolation: isolate;
  }

  .testimonial:hover {
    transform: translate(-6px, -6px);
    box-shadow: 6px 6px 0 var(--border);
    background: var(--bg);
  }

  /* Hover frame animation - top line */
  .testimonial::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--text);
    transition: width 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    z-index: 3;
  }

  .testimonial:hover::before {
    width: 100%;
  }

  /* Right line */
  .testimonial::after {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 2px;
    height: 0;
    background: var(--text);
    transition: height 0.3s cubic-bezier(0.16, 1, 0.3, 1) 0.08s;
    z-index: 3;
  }

  .testimonial:hover::after {
    height: 100%;
  }

  /* Bottom line via child element */
  .testimonial .testimonial-border-bottom {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 0;
    height: 2px;
    background: var(--text);
    transition: width 0.3s cubic-bezier(0.16, 1, 0.3, 1) 0.16s;
    z-index: 3;
  }

  .testimonial:hover .testimonial-border-bottom {
    width: 100%;
  }

  /* Left line via child element */
  .testimonial .testimonial-border-left {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 2px;
    height: 0;
    background: var(--text);
    transition: height 0.3s cubic-bezier(0.16, 1, 0.3, 1) 0.24s;
    z-index: 3;
  }

  .testimonial:hover .testimonial-border-left {
    height: 100%;
  }

  .quote-mark {
    position: absolute;
    top: var(--space-md);
    right: var(--space-md);
    font-family: Georgia, serif;
    font-size: 4rem;
    line-height: 1;
    color: var(--text);
    opacity: 0.1;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial:hover .quote-mark {
    opacity: 0.2;
    transform: scale(1.1) translateY(-4px);
  }

  .quote-text {
    font-size: clamp(1rem, 2vw, 1.0625rem);
    line-height: 1.6;
    color: var(--text);
    margin-bottom: var(--space-lg);
    font-style: normal;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial:hover .quote-text {
    transform: translateX(6px);
  }

  .quote-footer {
    padding-top: var(--space-md);
    border-top: 1px solid var(--border);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial:hover .quote-footer {
    transform: translateX(6px);
    border-color: var(--text);
  }

  .quote-author {
    display: block;
    font-size: 1rem;
    font-weight: 600;
    font-style: normal;
    margin-bottom: 0.25rem;
  }

  .quote-role {
    font-size: 0.875rem;
    color: var(--text-muted);
    transition: color 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial:hover .quote-role {
    color: var(--text);
  }
  
  /* Reveal animation */
  .reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity var(--transition-slow), transform var(--transition-slow);
    will-change: opacity, transform;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }
  
  @media (prefers-reduced-motion: reduce) {
    .reveal {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>
