---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout 
  title="404 — Página no encontrada | FRN.Q"
  description="La página que buscas no existe. Vuelve al inicio o contacta con nosotros."
>
  <main class="error-page">
    <div class="container">
      <div class="error-content">
        <div class="error-code" aria-hidden="true">
          <span class="code-digit">4</span>
          <span class="code-digit code-digit-alt">0</span>
          <span class="code-digit">4</span>
        </div>
        
        <h1 class="error-title">Página no encontrada</h1>
        <p class="error-desc">
          La ruta que buscas no existe o ha sido movida. 
          Pero no te preocupes, podemos llevarte de vuelta.
        </p>
        
        <div class="error-actions">
          <a href="/" class="error-btn btn-primary">
            <span>Volver al inicio</span>
            <span class="btn-arrow" aria-hidden="true">→</span>
          </a>
          <a href="#contacto" class="error-btn btn-secondary">
            <span>Contactar</span>
          </a>
        </div>
        
        <div class="error-visual" aria-hidden="true">
          <div class="visual-grid">
            {Array.from({ length: 16 }).map(() => (
              <span></span>
            ))}
          </div>
        </div>
      </div>
    </div>
  </main>
</MainLayout>

<style>
  .error-page {
    min-height: 100vh;
    min-height: 100dvh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 140px var(--space-md) var(--space-2xl);
    background: var(--bg);
  }

  .error-content {
    text-align: center;
    max-width: 600px;
  }

  .error-code {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-sm);
    margin-bottom: var(--space-xl);
    font-family: var(--font-mono);
    font-size: clamp(5rem, 15vw, 10rem);
    font-weight: 800;
    line-height: 1;
    letter-spacing: -0.05em;
  }

  .code-digit {
    display: inline-block;
    animation: float 3s ease-in-out infinite;
  }

  .code-digit:nth-child(2) {
    animation-delay: 0.2s;
  }

  .code-digit:nth-child(3) {
    animation-delay: 0.4s;
  }

  .code-digit-alt {
    opacity: 0.3;
    border: 4px solid var(--border);
    padding: 0 var(--space-sm);
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  @media (prefers-reduced-motion: reduce) {
    .code-digit {
      animation: none;
    }
  }

  .error-title {
    font-size: clamp(1.5rem, 5vw, 2.5rem);
    font-weight: 700;
    margin-bottom: var(--space-md);
    letter-spacing: -0.02em;
  }

  .error-desc {
    font-size: clamp(1rem, 2vw, 1.125rem);
    opacity: 0.7;
    line-height: 1.7;
    margin-bottom: var(--space-xl);
  }

  .error-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-md);
    margin-bottom: var(--space-3xl);
  }

  .error-btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-md) var(--space-xl);
    font-weight: 600;
    transition: 
      transform var(--transition-fast),
      box-shadow var(--transition-fast);
  }

  .btn-primary {
    background: var(--text);
    color: var(--bg);
    border: 2px solid var(--border);
    box-shadow: var(--shadow-brutal);
  }

  .btn-primary:hover {
    transform: translate(-3px, -3px);
    box-shadow: 7px 7px 0 var(--border);
  }
  
  .btn-primary:focus-visible {
    outline: 3px solid var(--text);
    outline-offset: 3px;
  }

  .btn-secondary {
    border: 2px dashed var(--border);
    opacity: 0.7;
  }

  .btn-secondary:hover {
    opacity: 1;
    border-style: solid;
    background: var(--bg-alt);
  }
  
  .btn-secondary:focus-visible {
    outline: 3px solid var(--text);
    outline-offset: 3px;
    opacity: 1;
  }

  .btn-arrow {
    transition: transform var(--transition-base);
  }

  .error-btn:hover .btn-arrow {
    transform: translateX(4px);
  }

  .error-visual {
    display: flex;
    justify-content: center;
    opacity: 0.1;
  }

  .visual-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-sm);
    width: 120px;
  }

  .visual-grid span {
    aspect-ratio: 1;
    background: var(--text);
    animation: pulse-grid 2s ease-in-out infinite;
  }

  .visual-grid span:nth-child(2) { animation-delay: 0.1s; }
  .visual-grid span:nth-child(3) { animation-delay: 0.2s; }
  .visual-grid span:nth-child(4) { animation-delay: 0.3s; }
  .visual-grid span:nth-child(5) { animation-delay: 0.4s; }
  .visual-grid span:nth-child(6) { animation-delay: 0.5s; }
  .visual-grid span:nth-child(7) { animation-delay: 0.6s; }
  .visual-grid span:nth-child(8) { animation-delay: 0.7s; }
  .visual-grid span:nth-child(9) { animation-delay: 0.8s; }
  .visual-grid span:nth-child(10) { animation-delay: 0.9s; }
  .visual-grid span:nth-child(11) { animation-delay: 1.0s; }
  .visual-grid span:nth-child(12) { animation-delay: 1.1s; }
  .visual-grid span:nth-child(13) { animation-delay: 1.2s; }
  .visual-grid span:nth-child(14) { animation-delay: 1.3s; }
  .visual-grid span:nth-child(15) { animation-delay: 1.4s; }
  .visual-grid span:nth-child(16) { animation-delay: 1.5s; }

  @keyframes pulse-grid {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    .visual-grid span {
      animation: none;
    }
  }
</style>
